name: Auto Fetch Proxies

on:
  schedule:
    # 将小时位改为 */3，代表每 3 小时的第 0 分钟执行一次
    - cron: '0 */3 * * *'
  workflow_dispatch: # 支持手动触发
  
permissions:
  contents: write

jobs:
  fetch-and-save-proxies:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install requests pyyaml

      - name: Run Python Script
        run: python fetch_proxies.py

      - - name: Commit and Push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # 使用 git add -A 会自动添加新生成的 txt 和 zip，同时记录被删除的历史 txt 文件
          git add -A
          
          if git diff --staged --quiet; then
            echo "没有提取到新节点或发生文件变更，跳过提交"
          else
            TZ='Asia/Shanghai' date_str=$(date +'%Y-%m-%d %H:%M:%S')
            git commit -m "Auto update Proxies & Archive: $date_str"
            git push
          fi
